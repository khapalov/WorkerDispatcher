image: microsoft/dotnet:latest

stages:
    - build
    - test

variables:
    test: "UnitTests"

before_script:
    - "cd source"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"

test:
    stage: test
    script: 
        - "cd $test"
        - "dotnet test"